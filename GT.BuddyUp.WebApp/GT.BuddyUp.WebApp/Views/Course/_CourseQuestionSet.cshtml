@model IEnumerable<GT.BuddyUp.WebApp.Models.Question>
<div id="questionRows">
    @foreach (var item in Model)
    {
        Html.RenderPartial("_CourseQuestion", item);
    }
</div>
@Html.ActionLink("Add another question", "BlankQuestionRow", null, new { id = "addQuestion" })
